# CourseWork2_Group_2
This is the Group Assignment for the Client side development.
This contain the html and css file for the form.
I have add an app.js which contain the notification function which shows when the form is completed and the button is used to submit and it also register the "cache.js". The Cache.js contain the service Worker function which is a cache storage system design to work by loading the website offline if the internet doesnot connect.
